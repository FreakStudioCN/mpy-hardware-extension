# Runtime + test dependencies for the mpyhw-api service. Session tokens use the
# stdlib (hmac/hashlib/base64), so there is no JWT package here.
fastapi>=0.115
uvicorn>=0.32
httpx>=0.28            # required by fastapi.testclient.TestClient
pydantic>=2
pytest>=8
pytest-asyncio>=0.25
