--- GENERATE PHASE PROTOCOL (V0) ---
You are RESUMING an in-progress build. upy-scaffold-plugin ALREADY ran: the project skeleton (firmware/, test/, project-manifest.json, conf.py, lib/) is ALREADY ON DISK, and the upstream manifest is in the RESOLVED DATA block below. Do NOT claim the project is empty, do NOT bail to result="failed"/next_phase="upy-analyze-plugin" or 'start from analyze', and do NOT re-run analyze — that destroys completed work. If unsure what exists, run file_operation(op="list") on the project root first; you will see the skeleton. Your ONLY job here is to generate the business code (drivers, tasks, main, tests) ON TOP of that skeleton, then finalize.
check_generate_plan (run by run_quality_gates with --require-plan --check-files) is the usual generate blocker. Write generate_plan.json COMPLETE up front so it passes in one pass; if a gate fails, read its error JSON and fix the plan — do NOT re-run the same gate more than twice.
- EVERY entry under a file section (drivers, tasks, tests, config_constants, main_assembly, resource_plan) that maps to a generated file MUST carry a project-relative `path` to that file, and that file MUST already be written before the check (e.g. {"name": "monitor_task", "path": "firmware/tasks/monitor_task.py"}). A bare name/description with no `path` fails the gate with GENERATE_PLAN_FILE_PATH_MISSING.
- data_flow_contract[] is REQUIRED for sensor/actuator/display/cloud/state-machine projects, and EACH entry MUST be an object with name (unique), producer, consumer, invariant (all non-empty strings) AND covered_by_tests (or test_path) naming the contract-test file that exercises it (e.g. "test/pc/test_monitor_task.py"); add storage for cross-stage flows. Missing coverage fails with GENERATE_PLAN_DATA_FLOW_TEST_MISSING.
- The `mpy_imports` gate (check_mpy_imports) scans ALL of firmware/ EXCEPT firmware/lib/ and HARD-FAILS (MPY_IMPORT_UNSUPPORTED) on host-only modules (subprocess, os.system, argparse, pathlib). Device firmware must contain ONLY on-device code. The host removes firmware/tools/ automatically before run_quality_gates executes; you do not need to delete it. Never place device code under firmware/tools/.
A failing gate is RECOVERABLE: read its error JSON, FIX the cause (write the missing generate_plan.json; delete firmware/tools/; remove a host import), and re-run that gate — do NOT emit partial just because a gate failed. result="partial", next_phase=null is ONLY for a genuinely unrecoverable state, never for a fixable gate error.
MANDATORY FINALIZE before you may emit result="success" — a success WITHOUT all of these is INVALID and check_phase_complete_consistency will reject it (it hard-fails unless session_state records a real git_commit):
  1. script_run(interpreter="shell", script="git add -A && git commit -m \"<one-line summary>\"") — a REAL git commit is REQUIRED (the project is already a git repo). This is NOT optional; do not declare success without it.
  2. script_run update_session_state.py to record that commit (git_commit + manifest_hash) into session_state.
  3. script_run check_phase_complete_consistency.py --phase-complete <file> --project-dir <root>; if it fails, FIX and re-run — never emit success while it fails.
  4. THEN emit phase_complete result="success", next_phase="upy-deploy-plugin", with manifest_content.generate.git.commit set to the real commit SHA. Do not keep regenerating code or re-validating already-passing artifacts; finalize exactly once.